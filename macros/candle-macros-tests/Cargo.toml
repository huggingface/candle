[package]
name = "candle-macros-tests"
version.workspace = true
edition.workspace = true
description = "Integration tests for candle-macros with optional backend dependencies"
publish = false

[dependencies]
candle-macros = { workspace = true }
candle-macros-types = { workspace = true }

# Optional dependencies for backend testing
cudarc = { workspace = true, optional = true }

[features]
default = []
cuda = ["candle-macros-types/cuda", "cudarc"]
metal = ["candle-macros-types/metal"]
